# SLM Integration Requirements
# Additional dependencies for Small Language Model integration

# Base SLM dependencies (required)
torch>=2.0.0
transformers>=4.35.0
tokenizers>=0.15.0
accelerate>=0.24.0

# Parameter Efficient Fine-tuning
peft>=0.6.0
bitsandbytes>=0.41.0

# Training and monitoring
datasets>=2.14.0
wandb>=0.16.0
tensorboard>=2.14.0

# Model optimization
optimum>=1.15.0
flash-attn>=2.4.0  # Optional: for faster attention

# Additional utilities
safetensors>=0.4.0
huggingface-hub>=0.19.0

# Development tools
jupyter>=1.0.0
ipywidgets>=8.0.0

# Note: Install with: pip install -r slm_requirements.txt
# GPU with at least 8GB VRAM recommended for training
